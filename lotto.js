/*
  로또 생성기
  로또 1장의 가격은 1000원이다.
  돈을 넣으면 살 수 있는 만큼 로또를 구매할 수 있다. (buyLottos 함수)
  각 로또 번호는 6개다.
*/

function buyLottos(money) {

  // 몇개를 발행했습니다
  // 1000으로 나누면 됨

  createLottosNumbers(number);

}

function createLottosNumbers(number) {

  // 인자만큼 반복문 수행
  for () {
    // 중복된 값을 제거해야함
    // 일단 indexOf를 사용
  }

  // 배열에 담긴 6개의 숫자 리턴

}

function setLuckyNumber(data) {
  // 6개의 값을 각각 하나씩 찾아야함
  // 생각이 잘 안남

  // 일단 한개의 데이터(숫자1개~6개까지)를 넘겨줌
  findWinNumber(data[i]);
}

function findWinNumber(num) {

  // 인자로 받아온 숫자를
  // 다른 카운트 배열을 만들어놓고
  // 해당 배열의 index를 똑같이 맞춰서
  // 만약 해당 숫자가 로또 배열에 있다면
  // 카운트 해주는 것임
  // 나중에 출력할 때 

}

function printWinningStatistics() {
  // 당첨통계 출력
  // switch-case 문을 이용해서
  // 아까 setLuckyNumber 에서 
  switch () {
    case 3:
      break;
    case 4:
      break;
    case 5:
      break;
    case 6:
      break;
    default:
      break;
  }

}